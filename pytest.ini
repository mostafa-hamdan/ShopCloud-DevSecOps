[pytest]
testpaths = tests
python_files = test_*.py
# test_api.py is a live integration test that requires a running
# stack; it's run separately via `docker compose --profile test`.
# The in-process suite (run via `pytest`) excludes it.
addopts = --ignore=tests/test_api.py
filterwarnings =
    ignore::DeprecationWarning
